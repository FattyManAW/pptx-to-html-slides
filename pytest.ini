[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: unit test (fast, no I/O)
    integration: integration test (may read fixtures)
    policy: dispatch policy tests
    due_date: due_date adapter tests
